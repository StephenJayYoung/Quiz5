
// @function reverseConcatArray
// @param {Array} array - the array to be reversed
// @returns {Array} newArray - the reversed array that was passed to the function

var array = ['Steve', 'Austin', 'Kali', 'Andrea', 'Leslie'];

var reverseConcatArray = function(array) {
	array.forEach.length;
	return reverseConcatArray;
};

array.prototype.of = function() {
	var newArray = reverseConcatArray;
	return newArray;
};

console.log(newArray);